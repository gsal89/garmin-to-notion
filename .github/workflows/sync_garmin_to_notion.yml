name: Sync Garmin to Notion (garth)

on:
  schedule:
    - cron: '3 1 * * *'   # 01:03 UTC = 09:03 AWST daily
  workflow_dispatch:

env:
  TZ: 'Australia/Perth'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt || pip install garth requests

      - name: Restore garth session files
        env:
          GARTH_OAUTH1_JSON: ${{ secrets.GARTH_OAUTH1_JSON }}
          GARTH_OAUTH2_JSON: ${{ secrets.GARTH_OAUTH2_JSON }}
        run: |
          mkdir -p "$RUNNER_TEMP/.garth"
          printf '%s' "$GARTH_OAUTH1_JSON" > "$RUNNER_TEMP/.garth/oauth1_token.json"
          printf '%s' "$GARTH_OAUTH2_JSON" > "$RUNNER_TEMP/.garth/oauth2_token.json"

      - name: Sync Activities
        env:
          GARTH_HOME: ${{ runner.temp }}/.garth
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DB_ID: ${{ secrets.NOTION_DB_ID }}           # Activities DB (required)
          GARMIN_ACTIVITIES_FETCH_LIMIT: ${{ vars.GARMIN_ACTIVITIES_FETCH_LIMIT }}
        run: |
          python garmin-activities.py

      - name: Update Personal Records
        env:
          GARTH_HOME: ${{ runner.temp }}/.garth
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_PR_DB_ID: ${{ secrets.NOTION_PR_DB_ID }}     # PR DB (required for PR step)
        run: |
          python personal-records.py

      - name: Sync Daily Steps (optional)
        if: ${{ secrets.NOTION_STEPS_DB_ID != '' }}
        env:
          GARTH_HOME: ${{ runner.temp }}/.garth
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_STEPS_DB_ID: ${{ secrets.NOTION_STEPS_DB_ID }}
        run: |
          python daily-steps.py

      - name: Sync Sleep (optional)
        if: ${{ secrets.NOTION_SLEEP_DB_ID != '' }}
        env:
          GARTH_HOME: ${{ runner.temp }}/.garth
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_SLEEP_DB_ID: ${{ secrets.NOTION_SLEEP_DB_ID }}
        run: |
          python sleep-data.py
